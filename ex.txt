Three immutable state change operations

1. Change the theme to 'Solar Light'

2. Log the user out

3. Clear contact data

reducer function
	three actions:
		change theme
		logout
		clear contact

Initial State to the Left

// theme action
state = reducer(initialState, action);
// logout action
state = reducer(state, action);
// clear contact action
state = reducer(state, action);

display all of the states